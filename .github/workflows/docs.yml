name: Docs
on:
  push:
    branches:
      - issue-161

jobs:
  docs:
    runs-on: ubuntu-latest

    steps:
      - uses: actions/checkout@v2
      - uses: actions/setup-node@v2
        with:
          node-version: '16'
      - name: install dependencies
        run: npm ci
      - name: build docs
        run: npm run docs

      - name: deploy
        if: github.ref == 'refs/heads/issue-161'
        uses: JamesIves/github-pages-deploy-action@4.1.4
        with:
          branch: gh-pages
          folder: build
