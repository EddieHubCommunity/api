import { KeyspaceInterceptor } from './keyspace.interceptor';

describe('KeyspaceInterceptor', () => {
  it('should be defined', () => {
    expect(new KeyspaceInterceptor()).toBeDefined();
  });
});
